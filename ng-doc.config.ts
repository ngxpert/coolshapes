import { NgDocConfiguration } from '@ng-doc/builder';

const config: NgDocConfiguration = {
  repoConfig: {
    url: 'https://github.com/ngxpert/coolshapes',
    mainBranch: 'main',
    releaseBranch: 'main',
  },
};

export default config;
